#!/usr/bin/env node

var library = require('./sass-migrator.dart.js');
library.load({
  path: require("path"),
  os: require("os"),
  fs: require("fs"),
});

library.cli_pkg_main_0_(process.argv.slice(2));
